import React from 'react';

const MapContainer = (props) => {
  return (
    <section
      id="map-container"
      role="application"
      aria-label="Map"
      >
    </section>
  );
}

export default MapContainer;