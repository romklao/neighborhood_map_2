import React from 'react';

const MapContainer = (props) => {
  return (
    <section
      id="map-container"
      role="application"
      aria-label="google map"
      >
    </section>
  );
}

export default MapContainer;